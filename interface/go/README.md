#LRC interface for golang(linux)
This code is to compile c code into go callable library function
#Execution steps
cd install ,execute:
  make install
#sample
The sample path:./sample
test data: ../../testdata

